# A project's AgentBastion configuration, as `agentbastion init` will write it.
providers:
  claude:
    capability_tier: native
gates:
  tests:
    suite: pytest -q
workflows:
  - name: feature
    steps:
      - role: implementer
