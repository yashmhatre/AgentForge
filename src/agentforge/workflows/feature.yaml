# The default Workflow: build something that was not there before.
#
# #11 adds the Security Role, #12 the Reviewer, and the Gates between them
# arrive with #9.
name: feature
steps:
  - role: implementer
  - role: tester
