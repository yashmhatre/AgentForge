# CLAUDE.md

This repo keeps its agent instructions in [`AGENTS.md`](./AGENTS.md), so that every agent tool reads the same file. Read it.

Nothing else lives here. Add repo-wide guidance to `AGENTS.md`; add it here only if it is genuinely specific to Claude Code and would confuse another tool.
